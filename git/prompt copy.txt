provide an exhaustive spec on agent powered by n8n, no code

features:
a salesman has sales reps that inputs data into different google sheets docs,
he wants to provide online forms to each user to input the data
the data input into the form is transferred to each responsibly google sheets docs,

each of the sheets is then merged into a major sheet defined by a crontab

he will then communicate with the agent via whatsapp to get:
a merged report Overview,
sample statistics,
a query based statistics
